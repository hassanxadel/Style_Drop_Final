{% comment %}
  Hero Section
  Converted from: src/sections/HeroSection.tsx

  Features:
  - Hero banner with headline and CTAs
  - "Same-day delivery" badge
  - Featured product card overlay (desktop only)
  - Trust features bar (4 features)
  - Fully responsive (mobile/desktop)
  - Configurable via Theme Editor

  Usage:
  Add to homepage template or use in Theme Editor
{% endcomment %}

{{ 'styledrop-hero-section.css' | asset_url | stylesheet_tag }}

{%- comment -%} Hero Section Container {%- endcomment -%}
<section class="hero-section">
  {%- comment -%} Main Hero Banner {%- endcomment -%}
  <div class="hero-section__main">
    <div class="container sd-hero__content-wrap">

      {%- comment -%} Top Row: MOVE FREELY | LIVE BOLDLY | FEEL ICONIC (Desktop only) {%- endcomment -%}
      <div class="sd-hero__tagline-row">
        <p class="sd-hero__tagline-text">
          {{ section.settings.tagline_1 | default: 'MOVE FREELY' }}
        </p>
        <p class="sd-hero__tagline-text">
          {{ section.settings.tagline_2 | default: 'LIVE BOLDLY' }}
        </p>
        <p class="sd-hero__tagline-text">
          {{ section.settings.tagline_3 | default: 'FEEL ICONIC' }}
        </p>
      </div>

      {%- comment -%} Content Row: Text + Image {%- endcomment -%}
      <div class="sd-hero__content-row">

        {%- comment -%} Left Column: Text Content {%- endcomment -%}
        <div class="sd-hero__left-col">

          {%- comment -%} SAME-DAY DELIVERY Badge {%- endcomment -%}
          <span class="sd-hero__delivery-badge">
            <svg class="sd-hero__delivery-badge-icon" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M13 2L4 14h6l-1 8 9-12h-6l1-8z"/>
            </svg>
            <span class="sd-hero__delivery-badge-text">
              {{ section.settings.badge_text | default: 'SAME-DAY DELIVERY' }}
            </span>
          </span>

          {%- comment -%} Main Headline {%- endcomment -%}
          <h1 class="hero-section__headline">
            <span class="block">{{ section.settings.headline_line_1 | default: 'WEAR WHAT' }}</span>
            <span class="block hero-section__headline-line--red">{{ section.settings.headline_line_2 | default: 'DROPS' }}</span>
            <span class="block">{{ section.settings.headline_line_3 | default: 'TODAY.' }}</span>
          </h1>

          {%- comment -%} Description {%- endcomment -%}
          <p class="hero-section__description">
            {{ section.settings.description | default: 'Multi-brand fashion delivered fast. Discover trending styles from the brands you love — with full transparency on pricing and returns.' }}
          </p>

          {%- comment -%} CTA Buttons {%- endcomment -%}
          <div class="sd-hero__cta-row">
            <a href="{{ section.settings.cta_primary_link | default: '/collections/all' }}">
              <button class="sd-hero__btn-primary">
                {{ section.settings.cta_primary_text | default: "Shop What's Trending" }}
              </button>
            </a>
            <a href="{{ section.settings.cta_secondary_link | default: '/collections/new-arrivals' }}" class="sd-hero__btn-secondary">
              {{ section.settings.cta_secondary_text | default: 'Explore New Arrivals' }}
              <span aria-hidden="true">→</span>
            </a>
          </div>
        </div>

        {%- comment -%} Right Column: Hero Image + Overlays {%- endcomment -%}
        <div class="sd-hero__right-col">

          {%- comment -%} Main Hero Image {%- endcomment -%}
          {%- if section.settings.hero_image -%}
            <img
              src="{{ section.settings.hero_image | img_url: '800x' }}"
              alt="{{ section.settings.hero_image.alt | default: 'Fashion model' }}"
              class="sd-hero__img"
              loading="eager"
            />
          {%- else -%}
            <div class="sd-hero__img-placeholder">
              <p>Upload hero image</p>
            </div>
          {%- endif -%}

          {%- comment -%} FAST DROP Badge Overlay {%- endcomment -%}
          {%- if section.settings.show_fast_drop_badge -%}
            <div class="sd-hero__fast-drop-badge">
              <div class="sd-hero__fast-drop-row">
                <span class="sd-hero__fast-drop-icon">⚡</span>
                <span class="sd-hero__fast-drop-label">FAST DROP</span>
              </div>
              <span class="sd-hero__fast-drop-sub">Delivered today</span>
            </div>
          {%- endif -%}

          {%- comment -%} Product Card Overlay (Desktop only) {%- endcomment -%}
          {%- if section.settings.featured_product != blank -%}
            {%- assign featured_product = all_products[section.settings.featured_product] -%}
            {%- if featured_product -%}
              <div class="sd-hero__product-card">
                {%- if featured_product.featured_image -%}
                  <img
                    src="{{ featured_product.featured_image | img_url: '200x' }}"
                    alt="{{ featured_product.title }}"
                    class="sd-hero__product-card-img"
                    width="200"
                    height="148"
                    loading="lazy"
                  />
                {%- endif -%}
                <p class="sd-hero__product-card-vendor">
                  {{ featured_product.vendor | upcase }}
                </p>
                <p class="sd-hero__product-card-title">
                  {{ featured_product.title | truncate: 20 }}
                </p>
                <div class="sd-hero__product-card-price-row">
                  <span class="sd-hero__product-card-price">
                    {{ featured_product.price | money }}
                  </span>
                  <span class="sd-hero__product-card-rtn">
                    FREE RTN
                  </span>
                </div>
              </div>
            {%- endif -%}
          {%- endif -%}
        </div>
      </div>
    </div>
  </div>

  {%- comment -%} Trust Features Bar {%- endcomment -%}
  <div class="hero-section__trust-bar">
    <div class="container sd-hero__trust-inner">
      <div class="sd-hero__trust-grid">

        {%- comment -%} Feature 1: Fast Delivery {%- endcomment -%}
        <div class="sd-hero__trust-item">
          <div class="sd-hero__trust-icon" style="background-color: rgba(232, 76, 30, 0.12);">
            <svg class="sd-hero__trust-icon-svg" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M13 2L4 14h6l-1 8 9-12h-6l1-8z"/>
            </svg>
          </div>
          <div>
            <p class="hero-section__trust-title">
              {{ section.settings.feature_1_title | default: 'Fast Delivery' }}
            </p>
            <p class="hero-section__trust-text">
              {{ section.settings.feature_1_desc | default: 'Same-day & next-day options' }}
            </p>
          </div>
        </div>

        {%- comment -%} Feature 2: Easy Returns {%- endcomment -%}
        <div class="sd-hero__trust-item">
          <div class="sd-hero__trust-icon" style="background-color: rgba(232, 76, 30, 0.12);">
            <svg class="sd-hero__trust-icon-svg" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.67">
              <path stroke-linecap="round" stroke-linejoin="round" d="M16.023 9.348h4.992v-.001M2.985 19.644v-4.992m0 0h4.992m-4.993 0l3.181 3.183a8.25 8.25 0 0013.803-3.7M4.031 9.865a8.25 8.25 0 0113.803-3.7l3.181 3.182m0-4.991v4.99"/>
            </svg>
          </div>
          <div>
            <p class="hero-section__trust-title">
              {{ section.settings.feature_2_title | default: 'Easy Returns' }}
            </p>
            <p class="hero-section__trust-text">
              {{ section.settings.feature_2_desc | default: 'Free 30-day hassle-free returns' }}
            </p>
          </div>
        </div>

        {%- comment -%} Feature 3: Secure Checkout {%- endcomment -%}
        <div class="sd-hero__trust-item">
          <div class="sd-hero__trust-icon" style="background-color: rgba(232, 76, 30, 0.12);">
            <svg class="sd-hero__trust-icon-svg" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="1.67">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z"/>
            </svg>
          </div>
          <div>
            <p class="hero-section__trust-title">
              {{ section.settings.feature_3_title | default: 'Secure Checkout' }}
            </p>
            <p class="hero-section__trust-text">
              {{ section.settings.feature_3_desc | default: '256-bit SSL encrypted payments' }}
            </p>
          </div>
        </div>

        {%- comment -%} Feature 4: Trusted Brands {%- endcomment -%}
        <div class="sd-hero__trust-item">
          <div class="sd-hero__trust-icon" style="background-color: rgba(232, 76, 30, 0.12);">
            <svg class="sd-hero__trust-icon-svg" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M12 2l2.39 4.84 5.34.78-3.87 3.77.91 5.32L12 14.77l-4.77 2.51.91-5.32L4.27 7.62l5.34-.78L12 2z"/>
            </svg>
          </div>
          <div>
            <p class="hero-section__trust-title">
              {{ section.settings.feature_4_title | default: 'Trusted Brands' }}
            </p>
            <p class="hero-section__trust-text">
              {{ section.settings.feature_4_desc | default: '200+ verified fashion brands' }}
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Hero Section",
  "tag": "section",
  "class": "section-hero",
  "settings": [
    {
      "type": "header",
      "content": "Top Taglines (Desktop only)"
    },
    {
      "type": "text",
      "id": "tagline_1",
      "label": "Tagline 1",
      "default": "MOVE FREELY"
    },
    {
      "type": "text",
      "id": "tagline_2",
      "label": "Tagline 2",
      "default": "LIVE BOLDLY"
    },
    {
      "type": "text",
      "id": "tagline_3",
      "label": "Tagline 3",
      "default": "FEEL ICONIC"
    },
    {
      "type": "header",
      "content": "Hero Content"
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "SAME-DAY DELIVERY"
    },
    {
      "type": "text",
      "id": "headline_line_1",
      "label": "Headline Line 1",
      "default": "WEAR WHAT"
    },
    {
      "type": "text",
      "id": "headline_line_2",
      "label": "Headline Line 2 (Red)",
      "default": "DROPS"
    },
    {
      "type": "text",
      "id": "headline_line_3",
      "label": "Headline Line 3",
      "default": "TODAY."
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Multi-brand fashion delivered fast. Discover trending styles from the brands you love — with full transparency on pricing and returns."
    },
    {
      "type": "header",
      "content": "Call to Action Buttons"
    },
    {
      "type": "text",
      "id": "cta_primary_text",
      "label": "Primary Button Text",
      "default": "Shop What's Trending"
    },
    {
      "type": "url",
      "id": "cta_primary_link",
      "label": "Primary Button Link",
      "default": "/collections/all"
    },
    {
      "type": "text",
      "id": "cta_secondary_text",
      "label": "Secondary Link Text",
      "default": "Explore New Arrivals"
    },
    {
      "type": "url",
      "id": "cta_secondary_link",
      "label": "Secondary Link URL",
      "default": "/collections/new-arrivals"
    },
    {
      "type": "header",
      "content": "Hero Image"
    },
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero Image"
    },
    {
      "type": "checkbox",
      "id": "show_fast_drop_badge",
      "label": "Show 'Fast Drop' Badge",
      "default": true
    },
    {
      "type": "product",
      "id": "featured_product",
      "label": "Featured Product Card (Desktop only)"
    },
    {
      "type": "header",
      "content": "Trust Features"
    },
    {
      "type": "text",
      "id": "feature_1_title",
      "label": "Feature 1 Title",
      "default": "Fast Delivery"
    },
    {
      "type": "text",
      "id": "feature_1_desc",
      "label": "Feature 1 Description",
      "default": "Same-day & next-day options"
    },
    {
      "type": "text",
      "id": "feature_2_title",
      "label": "Feature 2 Title",
      "default": "Easy Returns"
    },
    {
      "type": "text",
      "id": "feature_2_desc",
      "label": "Feature 2 Description",
      "default": "Free 30-day hassle-free returns"
    },
    {
      "type": "text",
      "id": "feature_3_title",
      "label": "Feature 3 Title",
      "default": "Secure Checkout"
    },
    {
      "type": "text",
      "id": "feature_3_desc",
      "label": "Feature 3 Description",
      "default": "256-bit SSL encrypted payments"
    },
    {
      "type": "text",
      "id": "feature_4_title",
      "label": "Feature 4 Title",
      "default": "Trusted Brands"
    },
    {
      "type": "text",
      "id": "feature_4_desc",
      "label": "Feature 4 Description",
      "default": "200+ verified fashion brands"
    }
  ],
  "presets": [
    {
      "name": "Hero Section"
    }
  ]
}
{% endschema %}
