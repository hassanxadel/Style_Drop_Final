{% comment %}
  StyleDrop Newsletter Section
  Converted from: src/sections/NewsletterSection.tsx
  
  Features:
  - Email signup with Shopify customer API
  - Dark background with large "DROP" text
  - Success/error messages
  - Privacy notice
  - Responsive layout
  
  Settings:
  - Heading text
  - Description
  - Button text
  - Success message
{% endcomment %}

<section class="bg-background py-16">
  <div class="container">
    <div class="relative overflow-hidden rounded-3xl bg-accent p-12 md:p-16">
      {%- comment -%} Large "DROP" Background Text {%- endcomment -%}
      <div class="absolute right-0 top-1/2 -translate-y-1/2 opacity-10 pointer-events-none">
        <span class="font-display text-[200px] md:text-[300px] font-normal text-white leading-none">
          DROP
        </span>
      </div>

      {%- comment -%} Content {%- endcomment -%}
      <div class="relative z-10 max-w-2xl">
        {%- comment -%} Badge {%- endcomment -%}
        <div class="flex items-center gap-2 mb-4">
          <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 24 24">
            <path d="M13 2L4 14h6l-1 8 9-12h-6l1-8z"/>
          </svg>
          <span class="font-condensed text-[11px] font-bold uppercase tracking-[1.54px] text-white">
            {{ section.settings.badge_text | default: "DROP INSIDER" }}
          </span>
        </div>

        {%- comment -%} Heading {%- endcomment -%}
        <h2 class="font-display text-5xl md:text-7xl font-normal text-white mb-6" style="letter-spacing: 0.02em;">
          {{ section.settings.heading | default: "BE FIRST TO KNOW<br>EVERY DROP." }}
        </h2>

        {%- comment -%} Description {%- endcomment -%}
        <p class="text-white/90 text-sm mb-8 max-w-lg">
          {{ section.settings.description | default: "Join 50K+ shoppers who get early access to new drops, exclusive deals, and style inspo — before anyone else." }}
        </p>

        {%- comment -%} Email Signup Form {%- endcomment -%}
        {%- form 'customer', id: 'NewsletterForm', class: 'flex flex-col sm:flex-row gap-3 max-w-xl' -%}
          <input type="hidden" name="contact[tags]" value="newsletter">
          
          {%- comment -%} Email Input {%- endcomment -%}
          <input
            type="email"
            name="contact[email]"
            id="NewsletterForm--{{ section.id }}"
            value="{{ form.email }}"
            placeholder="{{ section.settings.input_placeholder | default: 'your@email.com' }}"
            aria-required="true"
            autocorrect="off"
            autocapitalize="off"
            autocomplete="email"
            required
            {% if form.errors %}
              aria-invalid="true"
              aria-describedby="Newsletter-error--{{ section.id }}"
            {% elsif form.posted_successfully? %}
              aria-describedby="Newsletter-success--{{ section.id }}"
            {% endif %}
            class="flex-1 px-6 py-4 rounded-full bg-white/20 backdrop-blur border border-white/30 text-white placeholder:text-white/60 text-sm focus:outline-none focus:border-white/60 focus:bg-white/30"
          />

          {%- comment -%} Submit Button {%- endcomment -%}
          <button 
            type="submit"
            name="commit"
            class="px-8 py-4 bg-[#0D0D0D] text-white rounded-full font-condensed text-xs font-bold tracking-wider uppercase hover:bg-black transition-colors whitespace-nowrap"
          >
            {{ section.settings.button_text | default: "GET EARLY ACCESS →" }}
          </button>
        {%- endform -%}

        {%- comment -%} Success Message {%- endcomment -%}
        {%- if form.posted_successfully? -%}
          <p 
            id="Newsletter-success--{{ section.id }}" 
            class="text-white text-sm mt-4 font-semibold"
          >
            ✓ {{ section.settings.success_message | default: "Thanks for subscribing! Check your email for a welcome offer." }}
          </p>
        {%- endif -%}

        {%- comment -%} Error Message {%- endcomment -%}
        {%- if form.errors -%}
          <p 
            id="Newsletter-error--{{ section.id }}" 
            class="text-white text-sm mt-4"
          >
            {{ form.errors.translated_fields.email | capitalize }} {{ form.errors.messages.email }}
          </p>
        {%- endif -%}

        {%- comment -%} Privacy Notice {%- endcomment -%}
        <p class="text-xs text-white/70 mt-4">
          {{ section.settings.privacy_text | default: "No spam, ever. Unsubscribe anytime. We respect your inbox." }}
        </p>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Newsletter",
  "tag": "section",
  "class": "section-newsletter",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "DROP INSIDER"
    },
    {
      "type": "textarea",
      "id": "heading",
      "label": "Heading",
      "default": "BE FIRST TO KNOW<br>EVERY DROP.",
      "info": "Use <br> for line breaks"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Join 50K+ shoppers who get early access to new drops, exclusive deals, and style inspo — before anyone else."
    },
    {
      "type": "header",
      "content": "Form Settings"
    },
    {
      "type": "text",
      "id": "input_placeholder",
      "label": "Input Placeholder",
      "default": "your@email.com"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "GET EARLY ACCESS →"
    },
    {
      "type": "text",
      "id": "success_message",
      "label": "Success Message",
      "default": "Thanks for subscribing! Check your email for a welcome offer."
    },
    {
      "type": "text",
      "id": "privacy_text",
      "label": "Privacy Notice",
      "default": "No spam, ever. Unsubscribe anytime. We respect your inbox."
    }
  ],
  "presets": [
    {
      "name": "Newsletter"
    }
  ]
}
{% endschema %}
